# Database connection settings
db.url=jdbc:postgresql://localhost:5432/dealership
db.user=postgres
db.password=password

# Connection pool settings
db.max.pool.size=10
db.connection.timeout=30000

# Database initialization
db.init.on.start=true